# Nginx proxy server
- use of nginx to proxy requests between client and server

## Prerequisites
- Docker desktop installed

## Steps
1. Clone this repo
2. Open terminal, with the root folder of this project
3. Run `docker-compose up -d`
4. Access the server on `http://localhost:8080/`


